#ifndef PATCHER_MAJ_HPP
#define PATCHER_MAJ_HPP

namespace patcher
{
    class Maj
    {
        public:
            Maj();
            Maj(const Maj&) = delete;
            Maj& operator=(const Maj&) = delete;

        protected:

        private:
    };
}
#endif